<%= form_for(@mocropost) do |f| %>
  <% if @mocropost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mocropost.errors.count, "error") %> prohibited this mocropost from being saved:</h2>

      <ul>
      <% @mocropost.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
